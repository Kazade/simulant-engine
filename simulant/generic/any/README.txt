This is here temporarily until boost::any makes it into the standard (potentially C++14) and becomes available on Android.

This isn't the boost implementation, which required too many includes from the rest of boost. Instead I used the implementation from here:

http://mnmlstc.github.io/core/

