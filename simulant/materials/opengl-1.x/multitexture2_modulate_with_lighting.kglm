
BEGIN(PASS)
    SET(ITERATION ONCE)
    SET(FLAG LIGHTING OFF)
    SET(FLAG BLEND ALPHA)
END(PASS)

BEGIN(PASS)
    SET(ITERATION ONCE_PER_LIGHT)
    SET(FLAG LIGHTING ON)
    SET(FLAG BLEND ADD)
END(PASS)

