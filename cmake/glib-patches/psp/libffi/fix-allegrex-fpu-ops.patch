--- a/src/mips/o32.S
+++ b/src/mips/o32.S
@@ -94,7 +94,8 @@
 #ifndef __mips_soft_float
 pass_d:
 	bne	t0, FFI_ARGS_D, pass_f
-	l.d	$f12, 0*FFI_SIZEOF_ARG($sp)	# load $fp regs from args
+	lwc1	$f12, 0*FFI_SIZEOF_ARG($sp)	# load $fp regs from args (l.d expanded: allegrex has no l.d)
+	lwc1	$f13, 0*FFI_SIZEOF_ARG+4($sp)
 	REG_L	a2,   2*FFI_SIZEOF_ARG($sp)	# passing a double
 	REG_L	a3,   3*FFI_SIZEOF_ARG($sp)
 	b	call_it
@@ -109,8 +110,10 @@
 
 pass_d_d:		
 	bne	t0, FFI_ARGS_DD, pass_f_f
-	l.d	$f12, 0*FFI_SIZEOF_ARG($sp)	# load $fp regs from args
-	l.d	$f14, 2*FFI_SIZEOF_ARG($sp)	# passing two doubles
+	lwc1	$f12, 0*FFI_SIZEOF_ARG($sp)	# load $fp regs from args (l.d expanded: allegrex has no l.d)
+	lwc1	$f13, 0*FFI_SIZEOF_ARG+4($sp)
+	lwc1	$f14, 2*FFI_SIZEOF_ARG($sp)	# passing two doubles (l.d expanded: allegrex has no l.d)
+	lwc1	$f15, 2*FFI_SIZEOF_ARG+4($sp)
 	b	call_it
 
 pass_f_f:	
@@ -123,7 +126,8 @@
 
 pass_d_f:		
 	bne	t0, FFI_ARGS_DF, pass_f_d
-	l.d	$f12, 0*FFI_SIZEOF_ARG($sp)	# load $fp regs from args
+	lwc1	$f12, 0*FFI_SIZEOF_ARG($sp)	# load $fp regs from args (l.d expanded: allegrex has no l.d)
+	lwc1	$f13, 0*FFI_SIZEOF_ARG+4($sp)
 	l.s	$f14, 2*FFI_SIZEOF_ARG($sp)	# passing double and float
 	REG_L	a3,   3*FFI_SIZEOF_ARG($sp)
 	b	call_it
@@ -132,7 +136,8 @@
  # assume that the only other combination must be float then double
  #	bne	t0, FFI_ARGS_F_D, call_it
 	l.s	$f12, 0*FFI_SIZEOF_ARG($sp)	# load $fp regs from args
-	l.d	$f14, 2*FFI_SIZEOF_ARG($sp)	# passing double and float
+	lwc1	$f14, 2*FFI_SIZEOF_ARG($sp)	# passing double and float (l.d expanded: allegrex has no l.d)
+	lwc1	$f15, 2*FFI_SIZEOF_ARG+4($sp)
 
 #endif
 
@@ -179,7 +184,8 @@
 	jalr	t9
 	REG_L	t0, SIZEOF_FRAME + 4*FFI_SIZEOF_ARG($fp)
 #ifndef __mips_soft_float
-	s.d	$f0, 0(t0)
+	swc1	$f0, 0(t0)	# s.d expanded: allegrex has no s.d
+	swc1	$f1, 4(t0)
 #else
 	REG_S	v1, 4(t0)
 	REG_S	v0, 0(t0)
@@ -216,8 +222,10 @@
 	b	epilogue
 3:
 #ifndef __mips_soft_float
-	s.d	$f0, 0(t0)
-	s.d	$f2, 8(t0)
+	swc1	$f0, 0(t0)	# s.d expanded: allegrex has no s.d
+	swc1	$f1, 4(t0)
+	swc1	$f2, 8(t0)	# s.d expanded: allegrex has no s.d
+	swc1	$f3, 12(t0)
 #else
 	# FIXME: do nothing can pass all of the testsuite
 #endif
@@ -328,8 +336,10 @@
 	
 #ifndef __mips_soft_float
 	# Store all possible float/double registers.
-	s.d	$f12, FA_0_0_OFF2($fp)
-	s.d	$f14, FA_1_0_OFF2($fp)
+	swc1	$f12, FA_0_0_OFF2($fp)	# s.d expanded: allegrex has no s.d
+	swc1	$f13, FA_0_0_OFF2+4($fp)
+	swc1	$f14, FA_1_0_OFF2($fp)	# s.d expanded: allegrex has no s.d
+	swc1	$f15, FA_1_0_OFF2+4($fp)
 #endif
 1:
 	# prepare arguments for ffi_closure_mips_inner_O32
@@ -385,8 +395,10 @@
 	
 #ifndef __mips_soft_float
 	# Store all possible float/double registers.
-	s.d	$f12, FA_0_0_OFF2($fp)
-	s.d	$f14, FA_1_0_OFF2($fp)
+	swc1	$f12, FA_0_0_OFF2($fp)	# s.d expanded: allegrex has no s.d
+	swc1	$f13, FA_0_0_OFF2+4($fp)
+	swc1	$f14, FA_1_0_OFF2($fp)	# s.d expanded: allegrex has no s.d
+	swc1	$f15, FA_1_0_OFF2+4($fp)
 #endif
 1:	
 	# prepare arguments for ffi_closure_mips_inner_O32
@@ -420,7 +432,8 @@
 	beq	$8, $9, closure_done
 
 	li	$9, FFI_TYPE_DOUBLE
-	l.d	$f0, V0_OFF2($fp)
+	lwc1	$f0, V0_OFF2($fp)	# l.d expanded: allegrex has no l.d
+	lwc1	$f1, V0_OFF2+4($fp)
 	beq	$8, $9, closure_done
 
 	li      $9, FFI_TYPE_COMPLEX
@@ -432,8 +445,10 @@
 	beq     $3, $9, closure_done
 
 	li      $9, 16
-	l.d	$f0, V0_OFF2($fp)
-	l.d	$f2, (V0_OFF2+8)($fp)
+	lwc1	$f0, V0_OFF2($fp)	# l.d expanded: allegrex has no l.d
+	lwc1	$f1, V0_OFF2+4($fp)
+	lwc1	$f2, (V0_OFF2+8)($fp)	# l.d expanded: allegrex has no l.d
+	lwc1	$f3, (V0_OFF2+8+4)($fp)
 	beq     $3, $9, closure_done
 #endif
 1:	
