/* Generated by tools/cgen/cgen.py -- do not edit by hand.
 * Regenerate with: python3 tools/cgen/cgen.py
 */

#include <cstddef>
#include <cstdint>
#include <memory>
#include "simulant/nodes/camera.h"
#include "simulant/frustum.h"
#include "simulant/math/aabb.h"
#include "simulant/math/degrees.h"
#include "simulant/math/mat4.h"
#include "simulant/math/vec3.h"
#include "simulant/viewport.h"
#include "simulant/interfaces.h"
#include "simulant/utils/params.h"
#include "simulant/c/simulant_c.h"
#include "smlt_c_util.h"

extern "C" {

const char* smlt_camera_node_type_name(const smlt_camera_t* self) {
    return reinterpret_cast<const smlt::Camera*>(self)->node_type_name();
}

const smlt_aabb_t* smlt_camera_aabb(const smlt_camera_t* self) {
    return reinterpret_cast<const smlt_aabb_t*>(&(reinterpret_cast<const smlt::Camera*>(self)->aabb()));
}

smlt_aabb_t* smlt_camera_transformed_aabb(const smlt_camera_t* self) {
    return reinterpret_cast<smlt_aabb_t*>(new smlt::AABB(reinterpret_cast<const smlt::Camera*>(self)->transformed_aabb()));
}

const smlt_mat4_t* smlt_camera_view_matrix(const smlt_camera_t* self) {
    return reinterpret_cast<const smlt_mat4_t*>(&(reinterpret_cast<const smlt::Camera*>(self)->view_matrix()));
}

const smlt_mat4_t* smlt_camera_projection_matrix(const smlt_camera_t* self) {
    return reinterpret_cast<const smlt_mat4_t*>(&(reinterpret_cast<const smlt::Camera*>(self)->projection_matrix()));
}

const smlt_frustum_t* smlt_camera_frustum(const smlt_camera_t* self) {
    return reinterpret_cast<const smlt_frustum_t*>(&(reinterpret_cast<const smlt::Camera*>(self)->frustum()));
}

smlt_frustum_t* smlt_camera_frustum2(smlt_camera_t* self) {
    return reinterpret_cast<smlt_frustum_t*>(&(reinterpret_cast<smlt::Camera*>(self)->frustum()));
}

void smlt_camera_set_projection_matrix(smlt_camera_t* self, const smlt_mat4_t* matrix) {
    reinterpret_cast<smlt::Camera*>(self)->set_projection_matrix((*reinterpret_cast<const smlt::Mat4*>(matrix)));
}

void smlt_camera_set_perspective_projection(smlt_camera_t* self, const smlt_degrees_t* fov, float aspect, float near, float far) {
    reinterpret_cast<smlt::Camera*>(self)->set_perspective_projection((*reinterpret_cast<const smlt::Degrees*>(fov)), aspect, near, far);
}

void smlt_camera_set_orthographic_projection(smlt_camera_t* self, float left, float right, float bottom, float top, float near, float far) {
    reinterpret_cast<smlt::Camera*>(self)->set_orthographic_projection(left, right, bottom, top, near, far);
}

float smlt_camera_set_orthographic_projection_from_height(smlt_camera_t* self, float desired_height_in_units, float ratio) {
    return reinterpret_cast<smlt::Camera*>(self)->set_orthographic_projection_from_height(desired_height_in_units, ratio);
}

bool smlt_camera_on_create(smlt_camera_t* self, const smlt_params_t* params) {
    return reinterpret_cast<smlt::Camera*>(self)->on_create((*reinterpret_cast<const smlt::Params*>(params)));
}

} /* extern "C" */
