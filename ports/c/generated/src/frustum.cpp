/* Generated by tools/cgen/cgen.py -- do not edit by hand.
 * Regenerate with: python3 tools/cgen/cgen.py
 */

#include <cstddef>
#include <cstdint>
#include <memory>
#include "simulant/frustum.h"
#include "simulant/math/aabb.h"
#include "simulant/math/degrees.h"
#include "simulant/math/mat4.h"
#include "simulant/math/plane.h"
#include "simulant/math/vec3.h"
#include "simulant/c/simulant_c.h"
#include "smlt_c_util.h"

extern "C" {

void smlt_frustum_destroy(smlt_frustum_t* self) {
    delete reinterpret_cast<smlt::Frustum*>(self);
}

smlt_frustum_t* smlt_frustum_create(void) {
    return reinterpret_cast<smlt_frustum_t*>(new smlt::Frustum());
}

void smlt_frustum_build(smlt_frustum_t* self, const smlt_mat4_t* modelview_projection) {
    reinterpret_cast<smlt::Frustum*>(self)->build(reinterpret_cast<const smlt::Mat4*>(modelview_projection));
}

bool smlt_frustum_contains_point(const smlt_frustum_t* self, const smlt_vec3_t* point) {
    return reinterpret_cast<const smlt::Frustum*>(self)->contains_point((*reinterpret_cast<const smlt::Vec3*>(point)));
}

bool smlt_frustum_intersects_sphere(smlt_frustum_t* self, const smlt_vec3_t* pos, float diameter) {
    return reinterpret_cast<smlt::Frustum*>(self)->intersects_sphere((*reinterpret_cast<const smlt::Vec3*>(pos)), diameter);
}

bool smlt_frustum_intersects_aabb(const smlt_frustum_t* self, const smlt_aabb_t* box) {
    return reinterpret_cast<const smlt::Frustum*>(self)->intersects_aabb((*reinterpret_cast<const smlt::AABB*>(box)));
}

bool smlt_frustum_intersects_cube(const smlt_frustum_t* self, const smlt_vec3_t* center, float size) {
    return reinterpret_cast<const smlt::Frustum*>(self)->intersects_cube((*reinterpret_cast<const smlt::Vec3*>(center)), size);
}

bool smlt_frustum_initialized(const smlt_frustum_t* self) {
    return reinterpret_cast<const smlt::Frustum*>(self)->initialized();
}

float smlt_frustum_near_height(const smlt_frustum_t* self) {
    return reinterpret_cast<const smlt::Frustum*>(self)->near_height();
}

float smlt_frustum_far_height(const smlt_frustum_t* self) {
    return reinterpret_cast<const smlt::Frustum*>(self)->far_height();
}

float smlt_frustum_near_width(const smlt_frustum_t* self) {
    return reinterpret_cast<const smlt::Frustum*>(self)->near_width();
}

float smlt_frustum_far_width(const smlt_frustum_t* self) {
    return reinterpret_cast<const smlt::Frustum*>(self)->far_width();
}

float smlt_frustum_depth(const smlt_frustum_t* self) {
    return reinterpret_cast<const smlt::Frustum*>(self)->depth();
}

smlt_plane_t* smlt_frustum_plane(const smlt_frustum_t* self, smlt_frustum_plane_t p) {
    return reinterpret_cast<smlt_plane_t*>(new smlt::Plane(reinterpret_cast<const smlt::Frustum*>(self)->plane(static_cast<smlt::FrustumPlane>(p))));
}

smlt_vec3_t* smlt_frustum_direction(const smlt_frustum_t* self) {
    return reinterpret_cast<smlt_vec3_t*>(new smlt::Vec3(reinterpret_cast<const smlt::Frustum*>(self)->direction()));
}

smlt_vec3_t* smlt_frustum_up(const smlt_frustum_t* self) {
    return reinterpret_cast<smlt_vec3_t*>(new smlt::Vec3(reinterpret_cast<const smlt::Frustum*>(self)->up()));
}

smlt_vec3_t* smlt_frustum_right(const smlt_frustum_t* self) {
    return reinterpret_cast<smlt_vec3_t*>(new smlt::Vec3(reinterpret_cast<const smlt::Frustum*>(self)->right()));
}

float smlt_frustum_width_at_distance(const smlt_frustum_t* self, float distance) {
    return reinterpret_cast<const smlt::Frustum*>(self)->width_at_distance(distance);
}

float smlt_frustum_height_at_distance(const smlt_frustum_t* self, float distance) {
    return reinterpret_cast<const smlt::Frustum*>(self)->height_at_distance(distance);
}

smlt_degrees_t* smlt_frustum_field_of_view(const smlt_frustum_t* self) {
    return reinterpret_cast<smlt_degrees_t*>(new smlt::Degrees(reinterpret_cast<const smlt::Frustum*>(self)->field_of_view()));
}

float smlt_frustum_aspect_ratio(const smlt_frustum_t* self) {
    return reinterpret_cast<const smlt::Frustum*>(self)->aspect_ratio();
}

} /* extern "C" */
