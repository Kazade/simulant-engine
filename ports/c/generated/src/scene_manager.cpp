/* Generated by tools/cgen/cgen.py -- do not edit by hand.
 * Regenerate with: python3 tools/cgen/cgen.py
 */

#include <cstddef>
#include <cstdint>
#include <memory>
#include "simulant/scenes/scene_manager.h"
#include "simulant/path.h"
#include "simulant/scenes/scene.h"
#include "simulant/window.h"
#include "simulant/c/simulant_c.h"
#include "smlt_c_util.h"

extern "C" {

void smlt_scene_manager_destroy(smlt_scene_manager_t* self) {
    delete reinterpret_cast<smlt::SceneManager*>(self);
}

smlt_scene_manager_t* smlt_scene_manager_create(smlt_window_t* window) {
    return reinterpret_cast<smlt_scene_manager_t*>(new smlt::SceneManager(reinterpret_cast<smlt::Window*>(window)));
}

bool smlt_scene_manager_has_scene(const smlt_scene_manager_t* self, const char* route) {
    return reinterpret_cast<const smlt::SceneManager*>(self)->has_scene(std::string(route ? route : ""));
}

void smlt_scene_manager_unload(smlt_scene_manager_t* self, const char* route) {
    reinterpret_cast<smlt::SceneManager*>(self)->unload(std::string(route ? route : ""));
}

void smlt_scene_manager_destroy_all(smlt_scene_manager_t* self) {
    reinterpret_cast<smlt::SceneManager*>(self)->destroy_all();
}

void smlt_scene_manager_clean_destroyed_scenes(smlt_scene_manager_t* self) {
    reinterpret_cast<smlt::SceneManager*>(self)->clean_destroyed_scenes();
}

bool smlt_scene_manager_is_loaded(const smlt_scene_manager_t* self, const char* route) {
    return reinterpret_cast<const smlt::SceneManager*>(self)->is_loaded(std::string(route ? route : ""));
}

void smlt_scene_manager_reset(smlt_scene_manager_t* self) {
    reinterpret_cast<smlt::SceneManager*>(self)->reset();
}

bool smlt_scene_manager_scene_queued_for_activation(const smlt_scene_manager_t* self) {
    return reinterpret_cast<const smlt::SceneManager*>(self)->scene_queued_for_activation();
}

void smlt_scene_manager_unregister_scene(smlt_scene_manager_t* self, const char* name) {
    reinterpret_cast<smlt::SceneManager*>(self)->unregister_scene(std::string(name ? name : ""));
}

bool smlt_scene_manager_register_scene_from_source(smlt_scene_manager_t* self, const char* script_data, const char* class_name) {
    return reinterpret_cast<smlt::SceneManager*>(self)->register_scene(script_data, class_name);
}

bool smlt_scene_manager_register_scene_from_file(smlt_scene_manager_t* self, const smlt_path_t* script, const char* class_name) {
    return reinterpret_cast<smlt::SceneManager*>(self)->register_scene((*reinterpret_cast<const smlt::Path*>(script)), class_name);
}

} /* extern "C" */
